const sectionDiscover = document.getElementById('section-discover');
const navigateDiscover = document.querySelector('.navigation-discover');

sectionDiscover.addEventListener('click', () => {
    navigateDiscover.click();
})

const navigateGuide = document.querySelector('')